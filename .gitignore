- name: Commit SVG
  run: |
    git config --global user.name "github-actions"
    git config --global user.email "actions@github.com"
    git add -f shooting-game.svg
    git commit -m "Auto generate SVG" || echo "No changes"
    git push
